version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: houssie_postgres
    restart: always
    environment:
      POSTGRES_DB: houssie_db
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: